<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="AppPaint.Views.DrawingCanvasPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppPaint.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <Grid>
        <Grid.RowDefinitions>
 <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

  <!-- Toolbar -->
 <Border Grid.Row="0" 
    Background="{ThemeResource LayerFillColorDefaultBrush}"
   BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
      BorderThickness="0,0,0,1">
   <Grid Padding="12">
    <Grid.ColumnDefinitions>
     <ColumnDefinition Width="Auto"/>
  <ColumnDefinition Width="*"/>
       <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>

      <!-- Left Toolbar - Shape Tools -->
    <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
      <TextBlock Text="Tools:" VerticalAlignment="Center" Margin="0,0,12,0" FontWeight="SemiBold"/>
   
      <!-- Shape Buttons -->
      <ToggleButton x:Name="LineButton" Width="40" Height="40" ToolTipService.ToolTip="Line">
    <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE76E;"/>
            </ToggleButton>
     
       <ToggleButton x:Name="RectangleButton" Width="40" Height="40" ToolTipService.ToolTip="Rectangle">
  <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE91B;"/>
              </ToggleButton>
   
     <ToggleButton x:Name="OvalButton" Width="40" Height="40" ToolTipService.ToolTip="Oval">
           <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xF191;"/>
      </ToggleButton>
   
          <ToggleButton x:Name="CircleButton" Width="40" Height="40" ToolTipService.ToolTip="Circle">
      <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xEA3A;"/>
               </ToggleButton>
      
         <ToggleButton x:Name="TriangleButton" Width="40" Height="40" ToolTipService.ToolTip="Triangle">
       <TextBlock Text="△" FontSize="20" FontWeight="Bold"/>
   </ToggleButton>
       
      <ToggleButton x:Name="PolygonButton" Width="40" Height="40" ToolTipService.ToolTip="Polygon">
   <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE82F;"/>
  </ToggleButton>
        </StackPanel>

 <!-- Middle Toolbar - Color & Stroke -->
          <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" HorizontalAlignment="Center">
  <StackPanel Orientation="Horizontal" Spacing="8">
           <TextBlock Text="Color:" VerticalAlignment="Center"/>
  <ColorPicker x:Name="StrokeColorPicker" 
   ColorSpectrumShape="Ring"
      IsColorSliderVisible="False"
  IsColorChannelTextInputVisible="False"
   IsHexInputVisible="False"
         IsAlphaEnabled="False"
      Width="40" Height="40"/>
  </StackPanel>
         
            <Border Width="1" Background="{ThemeResource CardStrokeColorDefaultBrush}"/>
      
          <StackPanel Orientation="Horizontal" Spacing="8">
       <TextBlock Text="Thickness:" VerticalAlignment="Center"/>
   <Slider x:Name="StrokeThicknessSlider" 
Minimum="1" Maximum="20" Value="2" 
Width="120" VerticalAlignment="Center"/>
         <TextBlock Text="{x:Bind StrokeThicknessSlider.Value, Mode=OneWay}" 
   VerticalAlignment="Center" Width="30"/>
             </StackPanel>

  <Border Width="1" Background="{ThemeResource CardStrokeColorDefaultBrush}"/>
            
     <CheckBox x:Name="FillCheckBox" Content="Fill Shape"/>
       </StackPanel>

    <!-- Right Toolbar - Actions -->
    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
    <Button x:Name="ClearButton" Style="{StaticResource IconButtonStyle}" 
          ToolTipService.ToolTip="Clear Canvas">
    <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE894;"/>
    </Button>
     
      <Button x:Name="SaveButton" Style="{StaticResource AppAccentButtonStyle}"
      ToolTipService.ToolTip="Save as Template">
     <StackPanel Orientation="Horizontal" Spacing="8">
  <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE74E;"/>
  <TextBlock Text="Save"/>
            </StackPanel>
  </Button>
     </StackPanel>
  </Grid>
        </Border>

        <!-- Canvas Area -->
  <Grid Grid.Row="1" Padding="24">
    <Border Style="{StaticResource AppCardStyle}">
    <Grid>
  <!-- Canvas Background -->
         <Border x:Name="CanvasBackground" 
     Background="White"
            CornerRadius="4"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
   BorderThickness="1">
  
          <!-- Drawing Canvas -->
      <Canvas x:Name="DrawingCanvas" 
     Background="Transparent"
    Width="800" Height="600"/>
      </Border>

     <!-- Empty State -->
           <StackPanel x:Name="EmptyState" 
               Style="{StaticResource EmptyStateStyle}"
         Visibility="Collapsed">
     <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" 
        Glyph="&#xE70F;" 
            FontSize="64"
      Opacity="0.3"/>
          <TextBlock Text="Select a tool and start drawing" 
      Style="{StaticResource SubtitleTextBlockStyle}"
          Opacity="0.6"/>
           <TextBlock Text="Choose a shape from the toolbar above" 
        Opacity="0.5"/>
   </StackPanel>
        </Grid>
   </Border>
  </Grid>
    </Grid>
</Page>
