<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="AppPaint.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:AppPaint.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid Padding="24,0,24,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Header  -->
            <StackPanel
                Grid.Row="0"
                Margin="0,0,0,32"
                Spacing="12">
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <Border
                        Width="48"
                        Height="48"
                        Background="{ThemeResource AccentFillColorSecondaryBrush}"
                        CornerRadius="8">
                        <FontIcon
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource FluentIconsFontFamily}"
                            FontSize="24"
                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                            Glyph="&#xE9D9;" />
                    </Border>
                    <StackPanel VerticalAlignment="Center" Spacing="4">
                        <TextBlock
                            FontSize="28"
                            FontWeight="Bold"
                            Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                            Text="Dashboard" />
                        <TextBlock
                            FontSize="14"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="Overview of your drawings and usage statistics" />
                    </StackPanel>
                </StackPanel>
            </StackPanel>

            <!--  Statistics Cards  -->
            <Grid Grid.Row="1" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--  Total Drawings Card  -->
                <Border
                    Grid.Column="0"
                    Margin="0,0,12,0"
                    Padding="24"
                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                    CornerRadius="12">
                    <Border.Shadow>
                        <ThemeShadow />
                    </Border.Shadow>
                    <StackPanel Spacing="12">
                        <FontIcon
                            FontFamily="{StaticResource FluentIconsFontFamily}"
                            FontSize="36"
                            Foreground="White"
                            Glyph="&#xE8F1;" />
                        <TextBlock
                            FontSize="36"
                            FontWeight="Bold"
                            Foreground="White"
                            Text="{x:Bind ViewModel.TotalDrawings, Mode=OneWay}" />
                        <TextBlock
                            FontSize="14"
                            FontWeight="Medium"
                            Foreground="White"
                            Opacity="0.9"
                            Text="Total Drawings" />
                    </StackPanel>
                </Border>

                <!--  Total Shapes Card  -->
                <Border
                    Grid.Column="1"
                    Margin="0,0,12,0"
                    Padding="24"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="12">
                    <Border.Shadow>
                        <ThemeShadow />
                    </Border.Shadow>
                    <StackPanel Spacing="12">
                        <FontIcon
                            FontFamily="{StaticResource FluentIconsFontFamily}"
                            FontSize="36"
                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                            Glyph="&#xE91B;" />
                        <TextBlock
                            FontSize="36"
                            FontWeight="Bold"
                            Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                            Text="{x:Bind ViewModel.TotalShapes, Mode=OneWay}" />
                        <TextBlock
                            FontSize="14"
                            FontWeight="Medium"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="Total Shapes" />
                    </StackPanel>
                </Border>

                <!--  Total Profiles Card  -->
                <Border
                    Grid.Column="2"
                    Margin="0,0,12,0"
                    Padding="24"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="12">
                    <Border.Shadow>
                        <ThemeShadow />
                    </Border.Shadow>
                    <StackPanel Spacing="12">
                        <FontIcon
                            FontFamily="{StaticResource FluentIconsFontFamily}"
                            FontSize="36"
                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                            Glyph="&#xE77B;" />
                        <TextBlock
                            FontSize="36"
                            FontWeight="Bold"
                            Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                            Text="{x:Bind ViewModel.TotalProfiles, Mode=OneWay}" />
                        <TextBlock
                            FontSize="14"
                            FontWeight="Medium"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="Profiles" />
                    </StackPanel>
                </Border>

                <!--  Average Shapes Per Drawing Card  -->
                <Border
                    Grid.Column="3"
                    Padding="24"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="12">
                    <Border.Shadow>
                        <ThemeShadow />
                    </Border.Shadow>
                    <StackPanel Spacing="12">
                        <FontIcon
                            FontFamily="{StaticResource FluentIconsFontFamily}"
                            FontSize="36"
                            Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                            Glyph="&#xE9D9;" />
                        <TextBlock
                            FontSize="36"
                            FontWeight="Bold"
                            Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                            Text="{x:Bind ViewModel.AverageShapesPerDrawing, Mode=OneWay}" />
                        <TextBlock
                            FontSize="14"
                            FontWeight="Medium"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="Avg Shapes/Drawing" />
                    </StackPanel>
                </Border>
            </Grid>

            <!--  Charts and Details  -->
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--  Shape Type Distribution  -->
                <Border
                    Grid.Row="0"
                    Margin="0,0,0,20"
                    Padding="28"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="12">
                    <Border.Shadow>
                        <ThemeShadow />
                    </Border.Shadow>
                    <StackPanel Spacing="20">
                        <StackPanel Orientation="Horizontal" Spacing="16">
                            <Border
                                Width="40"
                                Height="40"
                                Background="{ThemeResource AccentFillColorTertiaryBrush}"
                                CornerRadius="8">
                                <FontIcon
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontFamily="{StaticResource FluentIconsFontFamily}"
                                    FontSize="20"
                                    Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                    Glyph="&#xE9D2;" />
                            </Border>
                            <TextBlock
                                VerticalAlignment="Center"
                                FontSize="20"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                Text="Shape Type Distribution" />
                        </StackPanel>

                        <ItemsRepeater ItemsSource="{x:Bind ViewModel.ShapeTypeStats, Mode=OneWay}">
                            <ItemsRepeater.Layout>
                                <StackLayout Spacing="16" />
                            </ItemsRepeater.Layout>
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="100" />
                                        </Grid.ColumnDefinitions>

                                        <!--  Shape Type Name  -->
                                        <TextBlock
                                            Grid.Column="0"
                                            VerticalAlignment="Center"
                                            FontSize="14"
                                            FontWeight="SemiBold"
                                            Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                            Text="{Binding ShapeTypeName}" />

                                        <!--  Progress Bar  -->
                                        <ProgressBar
                                            Grid.Column="1"
                                            Height="24"
                                            Margin="16,0"
                                            VerticalAlignment="Center"
                                            CornerRadius="6"
                                            Maximum="100"
                                            Value="{Binding Percentage}" />

                                        <!--  Count and Percentage  -->
                                        <StackPanel
                                            Grid.Column="2"
                                            HorizontalAlignment="Right"
                                            Orientation="Horizontal"
                                            Spacing="8">
                                            <TextBlock
                                                FontWeight="Bold"
                                                Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                                Text="{Binding Count}" />
                                            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                                <Run Text="(" />
                                                <Run Text="{Binding Percentage}" />
                                                <Run Text="%)" />
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>
                </Border>

                <!--  Recent Activity  -->
                <Border
                    Grid.Row="1"
                    Padding="28"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="12">
                    <Border.Shadow>
                        <ThemeShadow />
                    </Border.Shadow>
                    <StackPanel Spacing="20">
                        <StackPanel Orientation="Horizontal" Spacing="16">
                            <Border
                                Width="40"
                                Height="40"
                                Background="{ThemeResource AccentFillColorTertiaryBrush}"
                                CornerRadius="8">
                                <FontIcon
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontFamily="{StaticResource FluentIconsFontFamily}"
                                    FontSize="20"
                                    Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                    Glyph="&#xE81C;" />
                            </Border>
                            <TextBlock
                                VerticalAlignment="Center"
                                FontSize="20"
                                FontWeight="SemiBold"
                                Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                Text="Recent Drawings" />
                        </StackPanel>

                        <ListView
                            MaxHeight="350"
                            ItemsSource="{x:Bind ViewModel.RecentDrawings, Mode=OneWay}"
                            SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="16,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <!--  Icon  -->
                                        <Border
                                            Grid.Column="0"
                                            Width="40"
                                            Height="40"
                                            Margin="0,0,16,0"
                                            Background="{ThemeResource AccentFillColorTertiaryBrush}"
                                            CornerRadius="8">
                                            <FontIcon
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                FontFamily="{StaticResource FluentIconsFontFamily}"
                                                FontSize="20"
                                                Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                                Glyph="&#xE8F1;" />
                                        </Border>

                                        <!--  Info  -->
                                        <StackPanel Grid.Column="1" Spacing="6">
                                            <TextBlock
                                                FontSize="14"
                                                FontWeight="SemiBold"
                                                Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                                Text="{Binding Name}" />
                                            <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                                <Run Text="{Binding Shapes.Count}" />
                                                <Run Text="shapes • " />
                                                <Run Text="{Binding Width}" />
                                                <Run Text="×" />
                                                <Run Text="{Binding Height}" />
                                                <Run Text="px" />
                                            </TextBlock>
                                        </StackPanel>

                                        <!--  Date  -->
                                        <TextBlock
                                            Grid.Column="2"
                                            VerticalAlignment="Center"
                                            FontSize="12"
                                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                            Text="{Binding CreatedAt}" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
